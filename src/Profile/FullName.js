import React from "react";

const FullName = () => {
  const pStyle = {
    fontSize: "20px",
    fontWeight: "bold",
    color: "blue",
  };

  return <p style={pStyle}>Walid Khalfa</p>;
};

export default FullName;
