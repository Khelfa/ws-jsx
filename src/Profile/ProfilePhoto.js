import React from "react";

const ProfilePhoto = () => {
  const imgStyle = {
    border: "2px solid black",
  };

  return <img src="./ProfilePhoto.jpg" alt="Profile Photo" style={imgStyle} />;
};

export default ProfilePhoto;
